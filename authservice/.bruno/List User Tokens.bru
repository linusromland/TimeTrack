meta {
  name: List User Tokens
  type: http
  seq: 6
}

get {
  url: {{URL}}/auth/token/list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {
    "scope": "cli",
    "expiry": 24
  }
}

meta {
  name: Generate API Token
  type: http
  seq: 5
}

post {
  url: {{URL}}/auth/token/generate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

body:json {
  {
    "scope": "cli",
    "expiry": 24
  }
}
